# 通南巴项目部 · 承包商安全培训与隐患闭环系统 — 迭代记录

> 版本号说明：自 `2026.08.10` 起引入显式版本常量（`frontend/src/version.js` 与 `backend/src/version.js`）。
> 管理后台「设置 → 系统信息」可查看 前端版本 / 后端版本 / 构建时间。
> 此前迭代（Sprint0–Sprint3）仅记录于本文件与项目工作记忆，未使用 semver。

---

## 2026.08.17 · v2026.08.17（当前版本）
- **题库导入新增「Word 试卷直接导入」，支持主观题批量入库**：
  - 新增后端解析服务 `services/docxQuizImport.js`：用 `adm-zip` 读取「试题卷.docx」+「参考答案.docx」，按章节标题（选择题/判断题/简答题/案例分析题…）自动识别题型与每题分值；客观题答案归一化（字母 A-D；判断题 √/× → 正确/错误），括号内解析文字存入 `analysis`；主观题（简答/案例分析）整段参考答案存入 `answer`。
  - 新增 `POST /api/admin/quiz-import/import-docx`：接收 `questions`（必填）+ `answers`（可选）两个 docx，解析后写入 `t_question`，复用 Excel 导入的校验/报告与题库状态更新逻辑。
  - 前端 `QuizImportPage` 增加「Excel 模板 / Word 试卷」双模式切换：Word 模式上传试题卷+参考答案即可一键导入，结果展示题型分布。
  - **关键修复**：`t_question.answer` 字段由 `varchar(50)` 拓宽为 `text`，否则主观题参考答案（300–600 字）会被截断——这是此前主观题无法完整入库的根因。
  - 已用《通南巴项目部石油工程监督（甲方监督）考试试卷》实测：35 题（20 单选 + 10 判断 + 4 简答 + 1 案例分析）全部解析成功、0 失败，参考答案完整入库。

## 2026.08.15 · v2026.08.15
- **人员管理完善（录入/修改/导入/导出/批量处理全闭环）**：在既有"新增/编辑/Excel 导入"基础上补齐缺失能力。
  - 新增 `GET /api/admin/users/export`：按当前筛选导出人员信息 Excel（姓名/身份证/承包商/主管单位/手机/状态/录入时间），支持 keyword/unit/supervising_unit 筛选。
  - 新增 `DELETE /api/admin/users/:id`：单条删除人员。
  - 新增 `POST /api/admin/users/batch`：`{ids, action}` 批量 delete/enable/disable，参数校验 + 去重 + IN 占位符。
  - 前端 UsersPage：表格加复选框列（本页全选）、选中后出现批量操作条（批量启用/禁用/删除，删除带二次确认）、工具栏加"导出"按钮（按筛选下载 xlsx）、每行加"删除"按钮。
  - 全部操作成功后自动刷新并清空选择。

## 2026.08.14 · v2026.08.14
- **断点续答改为服务端持久化（修复"关门重登记录丢失"）**：原进度仅存设备 localStorage 且不绑定用户，微信/企业微信 webview 清缓存或换设备即丢失。现新增 `t_quiz_progress` 表按 `(user_id, scope, material_id, mode)` 落库；前端 mount 时优先从服务端恢复（回退本地缓存），作答/翻题/计时即时写本地+防抖(1.5s)写服务端，提交成功双端清除；恢复横幅仅在确有真实进度时才显示，杜绝空进度误报"已恢复"。
- 前后端 version.js 升至 2026.08.14
- **学习/练习答对自动跳题**：学习、练习模式下，作答且**答对**后自动跳转下一题（延时 800ms 显示对勾反馈）；答错停留当前题看解析/改答案；考试模式与简答题不自动跳。

## 2026.08.12 · v2026.08.12
- **移除练习答错提醒提示**：应手机端反馈（提示占用有限屏幕、题目需上下翻看不全），彻底移除学习/练习界面的"本次练习已答错 X 题，请点交卷录入错题库"提示（含此前顶部固定横幅及更早的题目卡片上方内联提示）。底部「交卷」录入错题库逻辑不受影响。

## 2026.08.11 · v2026.08.11
- （该版本曾将练习答错提醒移至顶部固定条，因占用手机屏幕、用户要求取消，已于 v2026.08.12 撤销。）

## 2026.08.10 · v2026.08.10
- **版本化（A 方案）**：前后端新增 `version.js` 单一可信源，设置页「系统信息」展示前端/后端版本与构建时间；后端新增公开接口 `GET /api/version`。
- **答题页布局修复**：「交卷/完成学习」浮窗上移（`bottom:128px → 200px`），解决与「下一题」导航按钮在右下角重叠。
- **错题库闭环加固**：常驻右下「交卷」按钮；答错红色横幅；提交失败明确报错、不假成功；JWT 有效期延长至 7 天。
- **学习/练习去除限时**：仅「考试」模式倒计时，学习/练习只显示已用时间。
- **首页集成培训登录**：主站首页末尾加姓名+身份证后4位表单，跳转 `/tnb/login` 自动登录。
- **空格键跳题**：学习/练习界面空格键下一题。

## Sprint 3（体系审核专项 / 超期闭环）
- 隐患日报 Excel 导入（图片按锚点行匹配），状态映射与幂等指纹。
- 隐患排查项目多选筛选、数据导出台帐/周报/月报。
- 错题库表 `t_wrong_question`（唯一键 user+question），答错 upsert、答对移出。
- `t_hazard` 扩展：业务归口/整改单位/录入人隔离/软删除、超期扫描每日幂等护栏。
- 出题质量量化校验（`t_quality_config` / `t_question_revision_log` / `t_quality_report`）。

## Sprint 2（隐患闭环 + 字典）
- 隐患闭环状态机 `reported→assigned→rectifying→verifying→closed`。
- 隐患分类/等级字典表、整改单位↔业务口关联、钉钉群@路由。
- 承包商单位维度表与甲方联系人三列。

## Sprint 1（隐患闭环 + 钉钉通知）
- 隐患上报/照片上传、超期自动通知与升级。
- 钉钉群机器人通知（Tier0）、员工待办（Tier1）。

## Sprint 0（基础脚手架）
- 复用 TNB-Training 底座（Node/Express + Vue3/Vite），不另起炉灶。
- 培训模块三模式 study/practice/exam，抽题与发布。
- 腾讯云 CVM 部署，Web/PWA 主入口 + 钉钉通知辅层。
